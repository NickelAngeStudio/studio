//! Contain window interactive properties tests.



/// Test settings window properties and if the changes were successfull.
pub fn window_properties_tests() {
    todo!()
    //let mut window = Window::new().unwrap();

    // Test window default properties.
    //test_window_default_properties(window.get_properties());
}